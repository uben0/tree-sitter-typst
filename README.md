# New Typst grammar for Tree-Sitter

Tests are automatically generated (https://github.com/uben0/tree-sitter-typst-utils), but the generated syntax tree must be modified. For instance for `raw` blocks.
